services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: insonia
      POSTGRES_USER: insonia
      POSTGRES_PASSWORD: insonia
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
